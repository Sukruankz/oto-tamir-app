name: oto_tamir_app
description: "B2B SaaS Oto Tamir Takip ve Muhasebe Yönetim Sistemi"
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6

  # Firebase
  firebase_core: ^3.6.0
  firebase_auth: ^5.3.1
  cloud_firestore: ^5.4.4
  cloud_functions: ^5.1.4

  # State management & routing
  flutter_riverpod: ^2.5.1
  go_router: ^14.2.7

  # Yardımcı
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
